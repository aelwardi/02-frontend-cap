export class Section {
    constructor(
        id?: any,
        titre?: string,
        type?: string,
        description?: string,
        file?: string,
        dateCreated?: Date,
        dateUpdated?: Date,
        tempsestimer?: any,
    ) {}
}
